import styled from 'styled-components';

const NavSpaceContainer = styled.div`
  flex: 1;
  display : flex;
  align-items : center;
  justify-content: flex-start;
  padding-left: 1em;
`;

export default NavSpaceContainer;